package utils

import models.Value
import models.operation.OperationArray
import models.operation.OperationVariable

object TypeValidator {
    fun validate(value: Value, variables: List<OperationVariable>, arrays: List<OperationArray>) {

    }
}